class ColorOption {
  final String colorName; // red, blue, yellow, green
  final int displayColor; // actual display color (Colors.red, etc.)

  ColorOption({required this.colorName, required this.displayColor});
}
